# 📝 Prompt Patterns & System Design

> เอกสารสำหรับเก็บรูปแบบการ prompt และ system design ของโปรเจค pim-shop

**อัปเดตล่าสุด:** 2026-01-22

---

## 📋 สารบัญ

- [Prompt Patterns](#prompt-patterns)
- [System Design](#system-design)
- [Current Implementation](#current-implementation)
- [Best Practices](#best-practices)

---

## 🤖 Prompt Patterns

### Code Generation

#### สร้าง Component ใหม่

```
สร้าง [Component Name] component โดยมี requirements:
- Props: [list props]
- Features: [list features]
- Styling: ใช้ styled-components ตาม @[docs/theme.md]
- ใช้ TypeScript
- รองรับ responsive design
```

#### ตัวอย่าง

```
สร้าง ProductCard component โดยมี requirements:
- Props: product (id, name, price, image, description)
- Features: แสดงรูปภาพ, ชื่อ, ราคา, ปุ่มเพิ่มตะกร้า
- Styling: ใช้ styled-components ตาม @[docs/theme.md]
- ใช้ TypeScript
- รองรับ responsive design
```

---

### API Integration

#### สร้าง API Route

```
สร้าง API route สำหรับ [resource] โดย:
- Method: [GET/POST/PUT/DELETE]
- Endpoint: /api/[path]
- Parameters: [list params]
- Response format: ตาม @[docs/api-documentation.md]
```

#### ตัวอย่าง

```
สร้าง API route สำหรับ products โดย:
- Method: GET
- Endpoint: /api/products
- Parameters: category, search, limit, offset
- Response format: ตาม @[docs/api-documentation.md]
```

---

### Authentication

#### เพิ่ม Protected Feature

```
ต้องการให้ [feature] ต้อง login ก่อน โดย:
- ถ้ายังไม่ login: [behavior]
- ถ้า login แล้ว: [behavior]
- ใช้ NextAuth ตาม @[docs/authentication.md]
```

---

### Bug Fixing

```
มีปัญหา [อธิบายปัญหา]

ไฟล์ที่เกี่ยวข้อง: [ชื่อไฟล์]
Error message: [error message ถ้ามี]
Expected behavior: [พฤติกรรมที่ต้องการ]
Actual behavior: [พฤติกรรมที่เกิดขึ้นจริง]

ช่วยวิเคราะห์และแก้ไขให้หน่อย
```

---

### Documentation

```
อัปเดต docs ให้ตรงกับสิ่งที่ทำ โดย:
@[docs/api-documentation.md]
@[docs/project-structure.md]
@[docs/authentication.md]
```

---

### Continue Session

```
อ่าน @[docs/session-summary.md] แล้วทำต่อจาก [Next Step ที่ต้องการ]
```

---

## 🏗️ System Design

### Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      Frontend (Next.js 15)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │  Pages      │  │  Components │  │  Styles                 │ │
│  │  (App Dir)  │  │  (React)    │  │  (styled-components)    │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
│                                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │  Hooks      │  │  Store      │  │  Types                  │ │
│  │  (React Q)  │  │  (Zustand)  │  │  (TypeScript)           │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Routes (Next.js)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  /api/auth  │  │  /api/menu  │  │ /api/products│            │
│  │  (NextAuth) │  │             │  │              │            │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Database (Future: Prisma)                     │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │
│  │  User   │  │ Product │  │  Order  │  │Category │           │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

---

### Database Design (Planned)

```
┌─────────────┐       ┌─────────────┐
│    User     │       │   Session   │
├─────────────┤       ├─────────────┤
│ id          │◄──────│ userId      │
│ email       │       │ sessionToken│
│ password    │       │ expires     │
│ name        │       └─────────────┘
│ image       │
│ createdAt   │
└─────────────┘
       │
       ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│  Category   │       │   Product   │       │    Order    │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │◄──────│ categoryId  │       │ id          │
│ name        │       │ id          │◄────┬─│ userId      │
│ slug        │       │ name        │     │ │ status      │
│ subLabel    │       │ price       │     │ │ total       │
│ order       │       │ stock       │     │ │ address     │
└─────────────┘       │ images[]    │     │ │ createdAt   │
                      └─────────────┘     │ └─────────────┘
                                          │
                                          │ ┌─────────────┐
                                          │ │  OrderItem  │
                                          │ ├─────────────┤
                                          └─│ orderId     │
                                            │ productId   │
                                            │ quantity    │
                                            │ price       │
                                            └─────────────┘
```

---

## 📦 Current Implementation

### Implemented APIs

| Endpoint | Method | Status | Description |
|----------|--------|--------|-------------|
| `/api/auth/[...nextauth]` | GET/POST | ✅ | NextAuth routes |
| `/api/menu` | GET | ✅ | Navigation menu items |
| `/api/products` | GET | ✅ | Product list with filters |
| `/api/products/:id` | GET | ✅ | Product detail |

### Implemented Pages

| Route | Status | Description |
|-------|--------|-------------|
| `/` | ✅ | Home page |
| `/category` | ✅ | All products |
| `/category/:slug` | ✅ | Products by category |
| `/product/:id` | ✅ | Product detail |
| `/admin/products` | ✅ | Product management (basic) |

### Implemented Components

| Component | Status | Description |
|-----------|--------|-------------|
| `Navbar` | ✅ | Navigation + User menu |
| `AuthModal` | ✅ | Login/Register/Reset |
| `ProductCard` | ✅ | Product card with link |
| `CartDrawer` | ✅ | Cart sidebar |
| `OrderModal` | ✅ | Checkout (protected) |
| `Footer` | ✅ | Footer section |
| `FloatingNav` | ✅ | Mobile floating nav |

---

## 📚 Best Practices

### Prompt Tips

1. **Reference Docs** - ใช้ `@[docs/xxx.md]` เพื่ออ้างอิง requirements
2. **Be Specific** - ระบุ requirements ให้ชัดเจน
3. **Provide Context** - บอก files ที่เกี่ยวข้อง
4. **Include Examples** - ยกตัวอย่าง input/output ที่ต้องการ

### Design Principles

1. **Theme Consistency** - ใช้ theme จาก `docs/theme.md` เสมอ
2. **Type Safety** - ใช้ TypeScript types ให้ครบถ้วน
3. **API First** - ออกแบบ API ก่อนสร้าง UI
4. **Mobile First** - ออกแบบ responsive ตั้งแต่ต้น

### File Organization

```
styles/storefront/[ComponentName].styles.ts  # Styles
components/storefront/[ComponentName].tsx    # Component
app/[route]/page.tsx                         # Page
app/api/[resource]/route.ts                  # API
types/[domain].ts                            # Types
```

---

*Last updated: 2026-01-22*
